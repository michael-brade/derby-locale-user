var lib = require('..');
var should = require('chai').should();

describe('derby-locale-user', function () {
  describe('middleware', function () {
    it('should be a function', function () {
      lib.should.be.a('function');
    });
  });
});
